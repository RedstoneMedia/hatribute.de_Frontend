import { ImageObjToImagePathPipe } from './image-obj-to-image-path.pipe';

describe('ImageObjToImagePathPipe', () => {
  it('create an instance', () => {
    const pipe = new ImageObjToImagePathPipe();
    expect(pipe).toBeTruthy();
  });
});
