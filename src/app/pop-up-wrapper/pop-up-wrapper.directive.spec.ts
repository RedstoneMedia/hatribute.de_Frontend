import { PopUpWrapperDirective } from './pop-up-wrapper.directive';

describe('PopUpWrapperDirective', () => {
  it('should create an instance', () => {
    const directive = new PopUpWrapperDirective();
    expect(directive).toBeTruthy();
  });
});
